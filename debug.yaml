- name: this playbook is to test the debug message
  hosts: nginx
  become: yes
  
  tasks:
    - name: install nginx
      tags:
        -installation
      ansible.builtin.yum:
        name: nginx
        state: installed

    - name: start and enable nginx
      ansible.builtin.service:
        name: nginx
        state: started
        enabled: yes
      